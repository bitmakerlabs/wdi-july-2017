<%= form_for @todo do |f| %>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="field">
    <%= f.submit data: { disable_with: false } %>
  </div>
<% end %>
