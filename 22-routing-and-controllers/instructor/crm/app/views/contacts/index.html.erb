<h1>Contacts Index</h1>

<%= link_to 'New Contact', new_contact_path %>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <%= link_to(contact.name, contact_path(contact) ) %>
    </li>
  <% end %>
</ul>

<!-- _url  : Use in Controllers -->
<!-- _path : Use in Views -->
